services:
  - type: web
    name: semester-collab
    runtime: node
    plan: free
    buildCommand: npm install --prefix server && cd client && npm install --include=dev && npx vite build
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
